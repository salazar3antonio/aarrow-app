<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_audit_employee_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{data_audit_employee_name}"
            android:textAppearance="@style/TextAppearance.AppCompat.Display1"
            tools:text="Employee Name" />

        <TextView
            android:id="@+id/tv_audit_employee_id"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{Integer.toString(data_audit_employee_id)}"
            android:textAppearance="@style/TextAppearance.AppCompat.Large"
            android:textSize="24sp"
            tools:text="Employee Id" />
    </LinearLayout>

    <data>

        <variable
            name="data_audit_employee_name"
            type="String" />

        <variable
            name="data_audit_employee_id"
            type="Integer" />

    </data>
</layout>