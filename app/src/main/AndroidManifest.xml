<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.aarrowapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewEmployeeActivity"
            android:label="New Employee" />
        <activity
            android:name=".EmployeeProfileActivity"
            android:label="Employee Profile" />
        <activity
            android:name=".EmployeeEditActivity"
            android:label="Edit Profile" />
        <activity
            android:name=".EmployeeAuditsActivity"
            android:label="Employee Audits" />
        <activity
            android:name=".NewAuditActivity"
            android:label="New Audit" />
    </application>

</manifest>